//Write a function expression to calculate the third side of a triangle when the user gives the first two sides

const pyTri = function(a, b) {
     c = (a * a) + (b * b) 
     console.log( c * c)
     return c * c
}
pyTri(2 , 5)